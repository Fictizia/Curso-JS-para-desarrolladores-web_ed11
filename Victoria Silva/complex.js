let firstNum = 1
let SecondNum = 1

let firstText = 'hola'
let SecondText = firstText

let first = true
let Second = first

firstNum = 5 
firstText = 'adios'
first = false

console.log(`firstNum vale : ${firstNum}`)
console.log(`seconNum vale : ${SecondNum}`)

console.log(`firstNum vale : ${firstText}`)
console.log(`seconNum vale : ${SecondText}`)

console.log(`firstNum vale : ${first}`)
console.log(`seconNum vale : ${Second}`)

